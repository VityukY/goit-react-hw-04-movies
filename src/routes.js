const routes = {
   home: '/',
   movies: '/movies',
   movieDetauls: '/movies/:movieId',
   movieQuery: 'movies?query=:query',
   cast: '/movies/:movieId/cast',
   reviews: '/movies/:movieId/reviews',
};

export default routes;
